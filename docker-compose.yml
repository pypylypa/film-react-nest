version: "3.3"

services:

  database:
    image: postgres:16
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db:/docker-entrypoint-initdb.d/
    networks:
      - backend-network
  backend:
    image: ghcr.io/pypylypa/film-react-nest/backend:review-2-for-sprint-17
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      - database
    ports:
      - "3000:3000"
    networks:
      - frontend-network
      - backend-network

  frontend:
    image: ghcr.io/pypylypa/film-react-nest/frontend:review-2-for-sprint-17
    env_file:
      - .env
    volumes:
      - frontend_dist:/dist

  nginx:
    image: ghcr.io/pypylypa/film-react-nest/nginx:review-2-for-sprint-17
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - frontend_dist:/usr/share/nginx/dist
    depends_on:
      - backend
    networks:
      - frontend-network

  pgadmin:
    image: dpage/pgadmin4
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:80"
    depends_on:
      - database
    networks:
      - backend-network

volumes:
  postgres_data:
  frontend_dist:

networks:
  frontend-network:
    driver: bridge
  backend-network:
    driver: bridge
